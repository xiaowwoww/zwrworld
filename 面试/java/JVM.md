JVM（Java Virtual Machine）内存结构在 Java 8 及之后版本中主要分为以下几个区域，每个区域有其特定的作用和生命周期。   
以下是 基于 **Java 8+ 的标准内存模型** （注意：Java 8 移除了永久代，引入了元空间）：

✅ **JVM 内存主要分为两大类：**

**一、线程私有区域（Thread-Private）**

每个线程独享，随线程创建而创建，随线程销毁而释放。

**1. 程序计数器（Program Counter Register）**

**• 作用：** 记录当前线程正在执行的字节码指令地址（行号）。
>如果执行的是 native 方法，则值为 undefined。
  
**• 特点：**
>唯一一个不会发生 OutOfMemoryError 的区域。
>
>线程私有，生命周期与线程一致。

**2. 虚拟机栈（Java Virtual Machine Stack）**

**• 作用：存储方法调用的栈帧（Stack Frame），每个方法调用都会创建一个栈帧，包含：**
>局部变量表（Local Variables）
>
>操作数栈（Operand Stack）
>
>动态链接（Dynamic Linking）
>
>方法出口（Return Address）

**• 异常：**
>栈深度超限 → StackOverflowError
>
>栈无法扩展（如 -Xss 设置过小且系统内存不足）→ OutOfMemoryError
>
**• 线程私有**
>⚠️ 注意：常说的“堆内存”和“栈内存”中的“栈”，就是指这个虚拟机栈。

**3. 本地方法栈（Native Method Stack）**

**• 作用：为 JVM 调用 native 方法（如 C/C++ 编写的 JNI 函数）服务。**
>有些 JVM（如 HotSpot）将它和虚拟机栈合并实现。
>
>同样可能抛出 StackOverflowError 或 OutOfMemoryError。

**二、线程共享区域（Shared by All Threads）**

**4. 堆（Heap） —— 最大、最重要的内存区域**

**• 作用：** 存放**几乎所有对象实例和数组**（JVM 规范规定对象必须在堆上分配，但 JIT 优化可能逃逸分析后栈上分配）。

**• GC 主战场：** 垃圾回收器主要管理此区域。

**• 结构（分代）：**

**• 新生代（Young Generation）：**
>Eden 区
>
>Survivor From / To（S0, S1）

**• 老年代（Old Generation）**

**参数控制：**
>-Xms：初始堆大小
>
>-Xmx：最大堆大小

**• 异常：** java.lang.OutOfMemoryError: Java heap space

**5. 方法区（Method Area） —— 逻辑概念，Java 8+ 由元空间实现**

**• 作用：** 存储**类的元数据信息**，包括：
>类结构（字段、方法、接口）
>
>运行时常量池（Runtime Constant Pool）
>
>静态变量（static variables）
>
>即时编译器（JIT）优化后的代码缓存等

**Java 8 之前**：实现为 **永久代（PermGen）**，位于堆内。

**Java 8 及以后**：改为 **元空间（Metaspace）**，使用**本地内存（Native Memory）**，不再受 JVM 堆大小限制。
>参数：-XX:MetaspaceSize, -XX:MaxMetaspaceSize

**异常：**
>Java 7 及以前：java.lang.OutOfMemoryError: PermGen space
>
>Java 8+：java.lang.OutOfMemoryError: Metaspace

>**💡 注意：** 字符串常量池在 Java 7 之后也从永久代移到了堆中。

**🧩 补充说明：运行时常量池（Runtime Constant Pool）**
>是方法区的一部分。
>
>存放编译期生成的各种字面量和符号引用（如 "hello"、类名、方法名等）。
>
>具有动态性：运行期间也可将新常量放入池中（如 String.intern()）。


**📊 总结表格**

|内存区域|线程私有/共享|作用|是否会发生 OOM|
|--|--|--|--|
|程序计数器|私有|记录字节码行号|❌不会|
|虚拟机栈|私有|存储方法栈帧（局部变量、操作数栈等）|✅ 会（StackOverflow / OOM）|
|本地方法栈|私有|支持 native 方法调用|✅ 会|
|堆（Heap）|共享|存放对象实例、数组|✅ 会（最常见 OOM）|
|方法区（元空间）|共享|存储类元数据、常量池、静态变量|✅ 会（Metaspace OOM）|

**🔚 关键记忆点**

**对象在堆上，方法在栈上。**

**堆 = 对象；栈 = 方法调用；方法区 = 类信息。**

**Java 8 没有永久代，只有元空间（在本地内存）。**

**字符串常量池在堆里（Java 7+）。**

理解 JVM 内存结构，是排查内存泄漏、调优 GC、分析 OOM 问题的基础。



---
---

**虚拟机栈（Java Virtual Machine Stack）**

在 Java 虚拟机（JVM）中，**栈帧（Stack Frame）** 是用于支持虚拟机进行方法调用和执行的数据结构。它是线程私有栈（Java Virtual Machine Stack）中的基本元素，每个方法从调用到执行完成的过程，都对应着一个栈帧在虚拟机栈中的入栈和出栈过程。

**1. 局部变量表 (Local Variables)**

**作用：** 用于存放方法参数和方法内部定义的局部变量。

**细节：**
>**存储内容：** 包括基本数据类型（boolean, byte, char, short, int, float, long, double）和对象引用（reference）。对象本身存储在堆中，这里只存指向堆的指针。
>
>**容量计算：** 以“变量槽（Slot）”为单位。long 和 double 类型占用 2 个 Slot，其他类型占用 1 个 Slot。
>
>**索引规则：** 索引从 0 开始。如果是实例方法（非 static），索引 0 默认存放 this 对象引用，随后依次是方法参数和局部变量。
>
>**生命周期：** 变量的作用域结束并不意味着 Slot 立即被回收，但逻辑上不再可用。Slot 可以被复用（例如两个互斥的局部变量可能共用同一个 Slot 以节省空间）。

**2. 操作数栈 (Operand Stack)**

**作用：** 作为方法执行过程中的工作区，用于存放计算过程中的中间结果和参与运算的操作数。

**细节：**
>**工作原理：** 基于“后进先出”（LIFO）原则。大部分字节码指令（如 iadd, ldc, invokevirtual）都是从操作数栈中弹出数据，运算后再将结果压入栈顶。
>
>**深度变化：** 方法刚开始时栈为空。随着字节码指令的执行，栈的深度会动态变化。
>
>**类型匹配：** 除了 long 和 double 占用 2 个栈单元外，其他数据类型占用 1 个栈单元。JVM 严格规定操作数栈中的数据必须与指令要求的类型严格匹配，否则会在编译期或运行期报错。
>
>**方法调用：** 在调用其他方法时，参数会被压入当前方法的操作数栈，然后传递给目标方法的局部变量表。

**3. 动态链接 (Dynamic Linking)**

**作用：** 指向运行时常量池中该栈帧所属方法的引用，用于支持方法调用过程中的动态绑定。

**细节：**
>**符号引用转直接引用：** 在 Class 文件中，方法调用通常使用符号引用（如类名、方法名、描述符）。在运行时，通过动态链接，将这些符号引用转换为直接引用（内存地址），以便直接调用方法。
>
>**多态支持：** 这是实现 Java 多态（重写/Override） 的关键机制。当调用一个虚方法（非 private, static, final, constructor）时，JVM 需要通过这个链接在运行时确定实际调用的具体实现版本（晚绑定）。
>
>**简化调用：** 使得每个栈帧不需要重新去常量池查找方法信息，而是直接持有指向常量池项的指针，提高执行效率。

**4. 方法返回地址 (Return Address)**

**作用：** 记录方法执行完成后，程序计数器（PC）应该跳转回的位置，即调用该方法的下一条指令的地址。

**细节：**
>**正常退出：** 如果方法是通过执行 return 指令正常退出的，返回地址会被用来恢复上层调用者的执行流。
>
>**异常退出：** 如果方法是因为抛出未捕获的异常而退出的，则不会使用这个返回地址来确定恢复点，而是通过异常处理表寻找匹配的异常处理器。
>
>**本质：** 它本质上保存了调用者栈帧中的 PC 寄存器值。当当前栈帧出栈（方法结束）时，PC 寄存器会被更新为这个值，从而让 CPU 继续执行调用者的代码。

**补充说明：附加信息**

除了上述四个核心部分，栈帧中通常还包含一些**附加信息**（这些信息在规范中没有强制定义具体位置，由虚拟机实现决定）：

>**调试信息：** 用于支持源代码行号映射，方便调试器定位错误行。
>
>**参数信息：** 在某些实现中，为了支持反射或调试，可能会额外存储参数的元数据。

**总结图解**

当一个方法被调用时：

>1.JVM 创建一个新的栈帧。
>
>2.**局部变量表** 初始化参数和局部变量。
>
>3.**动态链接** 指向常量池中的方法符号。
>
>4.**返回地址** 记录调用者的下一行代码位置。
>
>5.方法执行过程中，**操作数栈** 频繁进出数据进行计算。
>
>6.方法结束，栈帧出栈，根据 **返回地址** 恢复执行流，释放内存。



---
---

**堆（Heap）**

Java 堆（Heap）是 JVM 管理的内存中最大的一块，也是**垃圾收集器（Garbage Collector, GC）管理的主要区域**。由于现代 GC 大多基于“分代收集理论”（Generational Collection Theory），堆内存被逻辑上划分为不同的区域，以便针对不同生命周期的对象采用不同的回收策略。

以下是 Java 堆的详细结构、演变及 GC 策略介绍：

**一、堆内存的分代结构**

**1. 新生代（Young Generation）**

>**特点：** 存放刚创建的对象。绝大多数对象“朝生夕死”，生命周期极短。
>
>**GC 类型：Minor GC**（或 Young GC）。频率高，速度快。
>
>**组成：**

>**Eden 区（伊甸园）：**
>>**作用**：绝大多数新对象在这里分配内存。
>>
>>**比例**：通常占新生代的 80%（默认比例，可调整）。
>>
>>**机制：** 当 Eden 区满时，触发 Minor GC。

>**Survivor 区（幸存者区）：**
>>**组成：** 分为 From (S0) 和 To (S1) 两个大小相等的区域。
>>
>>**作用：** 存放经过一次 Minor GC 后仍然存活的对象。
>>
>>**机制（复制算法）：**
>>>1.任何时候，只有一个 Survivor 区（比如 From）在使用，另一个（To）是空的。
>>>
>>>2.GC 发生时，将 Eden 和 From 中存活的对象，一次性复制到 To 区。
>>>
>>>3.清理掉 Eden 和 From 区的所有对象。
>>>
>>>4.角色互换：原来的 To 变成新的 From，原来的 From 变成新的 To（清空等待下次使用）。
>>**年龄（Age）：** 对象每熬过一次 Minor GC，年龄 +1。当年龄达到阈值（默认 15，可通过 -XX:MaxTenuringThreshold 调整），对象将进入老年代。
>**注意：** 如果单次 GC 后存活对象太多，超过了 Survivor 区的容量，剩余对象会直接通过“分配担保机制”进入老年代。

**2. 老年代（Old Generation / Tenured Generation）**

>**特点：** 存放生命周期较长的对象。
>
>**来源：**
>>1.新生代中熬过多次 GC（达到年龄阈值）的对象。
>>
>>2.大对象（如长字符串、大数组），可直接通过 -XX:PretenureSizeThreshold 参数设置直接进入老年代，避免在新生代频繁复制。
>>
>>3.动态年龄判断：如果 Survivor 区中相同年龄的所有对象大小总和大于 Survivor 空间的一半，该年龄及以上的对象直接进入老年代。
>**GC 类型：Major GC** 或 **Full GC**。频率低，速度慢（因为对象多且存活率高）。

**3. 元空间（Metaspace）/ 永久代（Permanent Generation）**

这是存放类元数据（Class Metadata）的区域，**严格来说不属于堆内存**，但在逻辑上常与堆一起讨论。

**JDK 7 及之前：永久代（PermGen）**

>位置：属于堆的一部分。
>
>问题：大小固定（-XX:PermSize, -XX:MaxPermSize），容易导致 java.lang.OutOfMemoryError: PermGen space。字符串常量池也在其中（JDK 7 移到了堆中）。

**JDK 8 及之后：元空间（Metaspace）**

>位置：本地内存（Native Memory），不再占用堆空间。
>
>优势：默认受限于系统可用内存，不易溢出（除非物理内存耗尽）。
>
>内容：类的结构信息、方法代码、常量池（运行时常量池）、即时编译器编译后的代码等。
>
>GC：主要在 Full GC 时进行回收（当类加载器卸载时，对应的元数据会被回收）。

**二、对象流转过程（生命周期）**

1.新生：对象优先在 Eden 区分配。

2.第一次 GC：Eden 满 -> Minor GC -> 存活对象复制到 Survivor (From/To)，年龄=1。

3.后续 GC：每次 Minor GC，存活对象在 Survivor 间复制，年龄递增。

4.晋升：
>年龄达到阈值（默认 15） -> 进入 老年代。
>
>Survivor 区放不下 -> 直接进入 老年代（分配担保）。
>
>大对象 -> 直接进入 老年代。

5.老年代 GC：老年代满 -> 触发 Major GC / Full GC（通常会伴随一次 Minor GC）。

6.元空间：类加载时分配，类卸载时回收（通常在 Full GC 期间检查）。

**三、主流垃圾回收策略与算法**

不同的代使用不同的算法，以平衡吞吐量和停顿时间。

**1. 新生代算法：复制算法（Copying）**
原理：如上所述，利用 Survivor 区进行“存活对象复制”，直接清空原区域。

优点：没有标记和整理过程，效率极高；不会产生内存碎片。

缺点：内存利用率低（只有 50% 可用，因为要预留一个 Survivor）。

适用场景：对象死亡率极高的新生代。

**2. 老年代算法**

由于老年代对象存活率高，复制成本太大，通常采用以下两种：

**标记 - 清除（Mark-Sweep）**
>过程：先标记出所有需要回收的对象，然后统一清除。
>
>缺点：会产生大量内存碎片，导致大对象无法分配（即使总空间够，但没有连续空间）。
>
>代表收集器：CMS（Concurrent Mark Sweep，JDK 9 已废弃，但在旧系统常见）。

**标记 - 整理（Mark-Compact）**
>过程：标记存活对象，然后将它们向一端移动，最后清理边界外的内存。
>
>优点：无内存碎片。
>
>缺点：移动对象需要更新引用地址，耗时较长，需要 Stop-The-World (STW)。
>
>代表收集器：Serial Old, Parallel Old, G1 (Region 内), ZGC。

**四、关键 GC 术语解释**

**1.Minor GC / Young GC：**
>发生在新生代。
>
>频率快，停顿时间短。

**2.Major GC / Old GC：**
>发生在老年代。
>
>速度比 Minor GC 慢。

**3.Full GC：**
>清理整个堆（新生代 + 老年代 + 元空间）。
>
>触发原因：
>>老年代空间不足。
>>元空间不足。
>>System.gc() 被显式调用（不推荐）。
>>CMS 处理失败（Concurrent Mode Failure）。
>>大对象直接进入老年代但空间不足。
>影响：停顿时间最长（Stop-The-World），应尽量避免频繁发生。

**五、总结图示逻辑**

[新对象] 

   ↓
   
[Eden 区] --(满)--> [Minor GC] 

                      ↓
                      
            (存活) --> [Survivor From] --(年龄++ / 满)--> [老年代]
            
            (死亡) --> [清除]

[老年代] --(满)--> [Major/Full GC] (标记-整理/清除) --> [清除/整理]

[类加载] --> [元空间] --(类卸载/Full GC)--> [回收]

**核心思想：**
>分而治之：利用对象“朝生夕死”的特性，在新生代用低成本算法快速回收。
>
>空间换时间：新生代预留 Survivor 区换取极高的回收速度。
>
>动静分离：老年代用复杂算法处理长期存活对象，减少整体 GC 频率。

